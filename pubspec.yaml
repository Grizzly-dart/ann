name: grizzly_ann
description: Artifical neural network library for Dart.
# version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.15.0 <3.0.0'

dependencies:
  grizzly:

dev_dependencies:
  pedantic: ^1.11.1
  test: ^1.20.1

dependency_overrides:
  grizzly:
    path: ../grizzly
